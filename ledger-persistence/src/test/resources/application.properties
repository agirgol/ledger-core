# Flyway owns the schema; Hibernate only checks that it matches the mappings.
# Letting ddl-auto create tables would mean the tests exercise a schema the
# migrations never produced — including, crucially, without the append-only
# triggers.
spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true

spring.jpa.properties.hibernate.jdbc.time_zone=UTC
